---
title: Claude Code 한국어 입문 가이드
description: 코딩을 몰라도 따라 할 수 있는 Claude Code 입문서. 설치부터 MCP, Hooks, Skills, 서브에이전트까지.
---

import Link from 'next/link'

<div className="hero">
  <div className="hero-inner">
    <span className="hero-badge">Claude Code · 한국어 입문 · 2026</span>
    <h1 className="hero-title">
      코딩 없이 시작하는 <span className="hero-accent">Claude Code</span>
    </h1>
    <div className="hero-sub">
      터미널이 처음이라도 따라올 수 있는 가장 친절한 한국어 가이드.<br/>
      설치 · 명령어 · GitHub · MCP · Hooks · Skills · 서브에이전트까지 한 권으로.
    </div>
    <div className="hero-cta">
      <Link href="/start/before-you-start" className="btn btn-primary">
        지금 시작하기 →
      </Link>
      <Link href="/extras/windows-setup" className="btn btn-ghost">
        Windows 풀 셋업
      </Link>
    </div>
    <div className="hero-meta">
      <span>28개 섹션</span>
      <span>·</span>
      <span>코딩 무경험 OK</span>
      <span>·</span>
      <span>100% 한국어</span>
    </div>
    <div className="hero-author">제작 · 정금구</div>
  </div>
</div>

<section className="features">
  <h2 className="section-title">이 가이드의 특징</h2>
  <div className="cards">
    <div className="card">
      <div className="card-icon">🌱</div>
      <h3>코딩 0년차도 OK</h3>
      <p>"터미널이 뭐예요?" 부터 시작합니다. 어려운 용어 대신 일상 비유로 설명해요.</p>
    </div>
    <div className="card">
      <div className="card-icon">🛠</div>
      <h3>실전 예제 중심</h3>
      <p>"이런 상황엔 이렇게" 하는 실제 사용 예시를 모든 섹션에 포함했어요.</p>
    </div>
    <div className="card">
      <div className="card-icon">🪟</div>
      <h3>Windows 친화적</h3>
      <p>WSL/Git Bash/PowerShell 차이부터 설치까지 한국 사용자가 가장 막히는 구간을 풀가이드로.</p>
    </div>
    <div className="card">
      <div className="card-icon">🚀</div>
      <h3>고급 기능까지</h3>
      <p>MCP, Hooks, Skills, 서브에이전트 — Claude Code의 진짜 힘을 끌어내는 법.</p>
    </div>
    <div className="card">
      <div className="card-icon">💸</div>
      <h3>비용 관리</h3>
      <p>"토큰이 뭔데 돈이 빠져?" — 요금제 선택부터 절약 패턴까지.</p>
    </div>
    <div className="card">
      <div className="card-icon">🔧</div>
      <h3>트러블슈팅 FAQ</h3>
      <p>에러 메시지를 검색해도 한국어 답이 안 나올 때 펴보는 응급실.</p>
    </div>
  </div>
</section>

<section className="audience">
  <h2 className="section-title">이런 분께 추천해요</h2>
  <div className="audience-list">
    <div className="audience-item">
      <strong>비개발자</strong>
      <span>폴더 정리, 문서 자동화, 데이터 표 만들기 같은 반복 작업을 AI에게 시키고 싶은 분</span>
    </div>
    <div className="audience-item">
      <strong>입문 개발자</strong>
      <span>AI 페어 프로그래밍에 익숙해지고 싶은 분, 1인 개발자가 되고 싶은 분</span>
    </div>
    <div className="audience-item">
      <strong>중급 이상</strong>
      <span>MCP · Skills · 서브에이전트로 자기만의 자동화 워크스페이스를 짜고 싶은 분</span>
    </div>
  </div>
</section>

<section className="quickstart">
  <h2 className="section-title">3분 빠른 시작</h2>
  <div className="quickstart-grid">
    <div className="qs-step">
      <div className="qs-num">1</div>
      <h4>Node.js 설치</h4>
      <p>nodejs.org에서 LTS 버전 다운로드 후 설치</p>
    </div>
    <div className="qs-step">
      <div className="qs-num">2</div>
      <h4>Claude Code 설치</h4>
      <pre><code>npm install -g @anthropic-ai/claude-code</code></pre>
    </div>
    <div className="qs-step">
      <div className="qs-num">3</div>
      <h4>실행 + 로그인</h4>
      <pre><code>claude</code></pre>
      <p>작업할 폴더로 이동한 뒤 위 명령. 처음엔 브라우저 로그인.</p>
    </div>
    <div className="qs-step">
      <div className="qs-num">4</div>
      <h4>대화 시작</h4>
      <p>"이 폴더에 뭐가 있는지 알려줘" 같은 한국어로 부탁하면 됩니다.</p>
    </div>
  </div>
  <div className="quickstart-cta">
    <Link href="/start/install" className="btn btn-primary-sm">자세한 설치 가이드 →</Link>
  </div>
</section>

<section className="closing">
  <div className="closing-inner">
    <h2>준비됐다면, 첫 페이지부터.</h2>
    <p>거창하게 시작할 필요 없어요. "터미널 켜기"부터 같이 해봐요.</p>
    <Link href="/start/before-you-start" className="btn btn-primary-lg">
      00 시작하기 전에 →
    </Link>
  </div>
</section>

<style jsx>{`
  .hero {
    padding: 96px 24px 64px;
    text-align: center;
    background: radial-gradient(ellipse at top, rgba(255, 122, 89, 0.18), transparent 60%),
                radial-gradient(ellipse at bottom right, rgba(212, 100, 27, 0.12), transparent 50%);
    border-bottom: 1px solid rgba(127, 127, 127, 0.15);
  }
  .hero-inner {
    max-width: 880px;
    margin: 0 auto;
  }
  .hero-badge {
    display: inline-block;
    padding: 6px 14px;
    border-radius: 999px;
    font-size: 13px;
    font-weight: 600;
    background: rgba(255, 122, 89, 0.12);
    color: #ff7a59;
    border: 1px solid rgba(255, 122, 89, 0.3);
    margin-bottom: 28px;
    letter-spacing: 0.3px;
  }
  .hero-title {
    font-size: clamp(30px, 4.5vw, 52px);
    font-weight: 800;
    line-height: 1.35;
    margin: 0 0 22px;
    letter-spacing: -0.4px;
    word-break: keep-all;
  }
  .hero-accent {
    background: linear-gradient(135deg, #ff7a59, #d4641b);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .hero-sub {
    font-size: clamp(15px, 2vw, 18px);
    line-height: 1.7;
    opacity: 0.82;
    margin: 0 auto 36px;
    max-width: 640px;
  }
  .hero-cta {
    display: flex;
    gap: 12px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 32px;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    padding: 12px 24px;
    border-radius: 10px;
    font-weight: 600;
    font-size: 15px;
    text-decoration: none;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .btn:hover { transform: translateY(-1px); }
  .btn-primary {
    background: linear-gradient(135deg, #ff7a59, #d4641b);
    color: white;
    box-shadow: 0 6px 20px rgba(255, 122, 89, 0.3);
  }
  .btn-primary-sm {
    background: linear-gradient(135deg, #ff7a59, #d4641b);
    color: white;
    padding: 10px 18px;
    font-size: 14px;
    border-radius: 8px;
  }
  .btn-primary-lg {
    background: linear-gradient(135deg, #ff7a59, #d4641b);
    color: white;
    padding: 16px 32px;
    font-size: 17px;
    box-shadow: 0 8px 24px rgba(255, 122, 89, 0.35);
  }
  .btn-ghost {
    border: 1px solid rgba(127, 127, 127, 0.3);
    background: transparent;
  }
  .hero-meta {
    display: flex;
    gap: 10px;
    justify-content: center;
    font-size: 13px;
    opacity: 0.6;
    flex-wrap: wrap;
  }
  .hero-author {
    margin-top: 22px;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.4px;
    opacity: 0.75;
  }
  .features, .audience, .quickstart {
    max-width: 1100px;
    margin: 0 auto;
    padding: 72px 24px;
  }
  .section-title {
    text-align: center;
    font-size: clamp(24px, 3vw, 32px);
    font-weight: 800;
    margin: 0 0 40px;
    letter-spacing: -0.5px;
  }
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 18px;
  }
  .card {
    padding: 26px;
    border-radius: 14px;
    border: 1px solid rgba(127, 127, 127, 0.18);
    background: rgba(127, 127, 127, 0.04);
    transition: border-color 0.15s, transform 0.15s;
  }
  .card:hover {
    border-color: rgba(255, 122, 89, 0.4);
    transform: translateY(-2px);
  }
  .card-icon { font-size: 32px; margin-bottom: 14px; }
  .card h3 { margin: 0 0 8px; font-size: 17px; font-weight: 700; }
  .card p { margin: 0; font-size: 14px; line-height: 1.65; opacity: 0.78; }
  .audience-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 16px;
  }
  .audience-item {
    padding: 22px 24px;
    border-radius: 12px;
    background: rgba(127, 127, 127, 0.05);
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .audience-item strong { font-size: 16px; }
  .audience-item span { font-size: 14px; line-height: 1.6; opacity: 0.78; }
  .quickstart {
    background: rgba(127, 127, 127, 0.04);
    margin-bottom: 0;
  }
  .quickstart-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 16px;
    margin-bottom: 28px;
  }
  .qs-step {
    padding: 22px;
    border-radius: 12px;
    border: 1px solid rgba(127, 127, 127, 0.18);
    background: var(--background, white);
  }
  :global(.dark) .qs-step { background: rgba(255, 255, 255, 0.03); }
  .qs-num {
    width: 28px; height: 28px;
    border-radius: 8px;
    background: linear-gradient(135deg, #ff7a59, #d4641b);
    color: white;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: 14px;
    margin-bottom: 12px;
  }
  .qs-step h4 { margin: 0 0 8px; font-size: 15px; font-weight: 700; }
  .qs-step p { margin: 8px 0 0; font-size: 13px; line-height: 1.6; opacity: 0.78; }
  .qs-step pre {
    margin: 6px 0 0;
    padding: 10px 12px;
    background: rgba(0, 0, 0, 0.06);
    border-radius: 6px;
    font-size: 13px;
    overflow-x: auto;
  }
  :global(.dark) .qs-step pre { background: rgba(255, 255, 255, 0.06); }
  .quickstart-cta { text-align: center; }
  .closing {
    padding: 96px 24px;
    text-align: center;
    background: radial-gradient(ellipse at center, rgba(255, 122, 89, 0.1), transparent 70%);
  }
  .closing-inner { max-width: 600px; margin: 0 auto; }
  .closing h2 {
    font-size: clamp(24px, 4vw, 36px);
    font-weight: 800;
    margin: 0 0 14px;
    letter-spacing: -0.8px;
  }
  .closing p { font-size: 16px; opacity: 0.78; margin: 0 0 28px; }
`}</style>
